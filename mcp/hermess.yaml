# Copy this block into ~/.hermes/config.yaml, or run scripts/setup-hermess.sh.
# The MCP server must already be running with .venv/bin/llm-wiki.
mcp_servers:
  llm_wiki:
    url: "http://127.0.0.1:9999/mcp"
    timeout: 120
    connect_timeout: 30
