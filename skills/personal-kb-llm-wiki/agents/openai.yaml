interface:
  display_name: "Personal KB LLM Wiki"
  short_description: "Maintain an Obsidian/Markdown LLM Wiki through personal-kb-mcp."
  default_prompt: "Use the personal-kb-llm-wiki skill and the personal_kb MCP server."
policy:
  allow_implicit_invocation: true
dependencies:
  tools:
    - type: "mcp"
      value: "personal_kb"
      description: "Personal KB MCP Streamable HTTP server"
      transport: "streamable_http"
      url: "http://127.0.0.1:9999/mcp"
